<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cardgame">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:name=".utils.MySPapp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Poke War!"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CardGame">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAUHOiyuzugK3qtehk_H43ytlO-w-6XvAI"/>

        <activity
            android:name=".activities.MenuActivity"
            android:screenOrientation="sensorLandscape"
            android:launchMode = "singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.PreGameActivity"
            android:screenOrientation="sensorLandscape"
            android:launchMode = "singleTop"/>
        <activity
            android:name=".activities.TopTenActivity"
            android:screenOrientation="sensorLandscape"
            android:launchMode = "singleTop"/>
        <activity
            android:name=".activities.WinnerActivity"
            android:screenOrientation="sensorLandscape"
            android:launchMode = "singleTop"/>
        <activity
            android:name=".activities.GameActivity"
            android:screenOrientation="sensorLandscape"
            android:launchMode = "singleTop"/>
    </application>

</manifest>